"""Bilateral dyadic instrumentation contracts and analysis (Plan 4).

This package normalizes negotiation records by actor turn rather than by a
privileged "main" agent, so text-only, one-sided white-box, and bilateral
white-box detectors can be compared without role or dyad leakage.
"""
