"""Monitor-aware adversary evaluation scaffolding (Plan 6).

Threat models are frozen manifests; adaptive tiers gain capabilities
cumulatively; every evaluation binds to a frozen monitor hash so a monitor
cannot drift while being attacked. This is a controlled safety evaluation
harness over synthetic negotiations only.
"""
