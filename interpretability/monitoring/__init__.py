"""Deception monitors, calibration, and control protocols (Plan 6).

Monitors consume view-gated inputs (no data may be present without its view
being granted), emit normalized suspicion with explicit coverage, and are
frozen by content hash before any adaptive evaluation may target them.
"""
